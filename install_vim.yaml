---
 - name: Install vim
   hosts: all
   tasks:
     - name: Install vim on Centos 7
       when: asnible_distribution == "CentOS"
       become: true
       become_user: centos
       yum:
         name: vim
         state: present